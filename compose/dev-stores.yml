# ⛔ DEPRECATED — 사용하지 않는다.
#
# 개인 PG/Redis 를 호스트 레벨 sibling 컨테이너로 띄우고 공유 piascope 네트워크에
# 붙이던 방식(= 호스트 인프라 꼬임의 축소판)은 폐지됐다. 새 격리 모델에서 개인
# 테스트 인프라는 '개발자 컨테이너 안'에서 로컬 프로세스로 띄운다:
#
#   devstores up      # 컨테이너 안 · 127.0.0.1:6379(redis) · 127.0.0.1:5432(pg)
#
# 구현: docker/devstores.sh (base 이미지에 /usr/local/bin/devstores 로 구움)
# 배경: docs/shared-infra-rules.md
